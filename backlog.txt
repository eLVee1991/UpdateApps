Problems:

Created a check_appstore_apps() function and get a list returned. But the filtered_apps() function doesn't filter them out.
To see this error uncheck all the #print(app.rstrip() + " is blacklisted!") in both functions.
